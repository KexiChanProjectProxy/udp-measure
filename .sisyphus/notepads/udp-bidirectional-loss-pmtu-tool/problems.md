# Remaining Problems

None. The F2 blocking issue is resolved.

## Resolved
- Cross-package UDP port collision between `internal/control` and `tests/integration` is fixed
- `go test ./... -count=1` passes reliably
- `go build ./...` passes
